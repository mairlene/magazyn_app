# Root example env
# Not tracked by git; copy to .env and fill real values if needed

# API / server
DATABASE_URL=postgresql://user:password@localhost:5432/magazyn_db
JWT_SECRET=change_this_secret
PORT=5000
NODE_ENV=development

# Optional: client API URL (if not using proxy in development)
REACT_APP_API_URL=http://localhost:5000
# Example environment variables for magazyn_app
# Copy to .env and fill with real values (DO NOT COMMIT .env)

# Client
REACT_APP_API_URL=
REACT_APP_API_KEY=

# Node / Server
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://user:password@host:5432/dbname
JWT_SECRET=

# Optional
SENTRY_DSN=

# Note: remove any secrets from repository and rotate leaked keys immediately.
